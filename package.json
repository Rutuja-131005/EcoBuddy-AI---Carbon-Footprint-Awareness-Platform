{
  "name": "ecobuddy-ai-carbon-footprint-platform",
  "version": "1.0.0",
  "description": "Full-stack carbon footprint awareness platform built with React, Express, and MongoDB.",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "test:coverage": "npm run test:coverage --prefix backend && npm run test:coverage --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
