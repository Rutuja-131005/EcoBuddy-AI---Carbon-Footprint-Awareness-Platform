const CATEGORIES = [
  "Transport",
  "Electricity",
  "Food",
  "Water",
  "Shopping",
  "Waste"
];

const GOAL_STATUSES = ["active", "completed"];

const RECOMMENDATION_SOURCES = ["seed", "dynamic"];

module.exports = {
  CATEGORIES,
  GOAL_STATUSES,
  RECOMMENDATION_SOURCES
};
